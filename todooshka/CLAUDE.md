# Todooshka

- UI components: `import { … } from '@monorepo/ui-kit'`
- Before building any page: read `.ai-kit/kit.json` + the relevant `.ai-kit/<Component>.kit.md` files
- Page generation: use skill `/pixso-page-gen` (install from `skills/pixso-page-gen/`)
- Sync contract with Pixso/ui-kit: use skill `/sync-ai-map` (install from `skills/sync-ai-map/`)
- Design tool: Pixso Desktop with local MCP (`mcp__pixso-desktop__*`); node GUID = `item-id` query param of the Pixso URL
